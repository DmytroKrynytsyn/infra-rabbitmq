apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbitmq
  namespace: rabbitmq
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}